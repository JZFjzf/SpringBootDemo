<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.choimroc.demo.application.example.mapper.ExampleMapper">

    <resultMap id="ExampleBody" type="com.choimroc.demo.application.example.body.ExampleBody" />

    <resultMap id="total" type="java.lang.Integer">
        <result column="total" />
    </resultMap>
    <update id="updateExample"></update>

    <select id="selectForPage" resultType="java.util.List" resultMap="ExampleBody,total">
        <![CDATA[

        SELECT SQL_CALC_FOUND_ROWS * FROM `example`
        WHERE id =#{id}
        <if test="name!=null and name!=''">
            AND `name` LIKE concat('%',#{name},'%')
        </if>
        <if test="startDate!=null and startDate!=''">
            AND create_time>=#{startDate}
        </if>
        <if test="endDate!=null and endDate!=''">
            AND create_time < #{endDate}
        </if>
        order by id DESC
        LIMIT #{current},#{pageSize};

        SELECT FOUND_ROWS() as total;

        ]]>
    </select>

    <insert id="insertReturnKey"
        parameterType="com.choimroc.demo.application.example.entity.Example" useGeneratedKeys="true"
        keyProperty="id">

    </insert>
</mapper>
